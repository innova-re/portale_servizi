{% extends '::base.html.twig' %}

{% block inner_content -%}

    <table class="table table-hover">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nome</th>
                <th>Descrizione</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('afferenza_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nome }}</td>
                <td>{{ entity.descrizione }}</td>
                <td>
                    <a class="btn" href="{{ path('afferenza_show', { 'id': entity.id }) }}">show</a>
                    <a class="btn" href="{{ path('afferenza_edit', { 'id': entity.id }) }}">edit</a>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <a class="btn" href="{{ path('afferenza_new') }}">
        Create a new entry
    </a>
    {% endblock %}
