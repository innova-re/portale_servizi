{% extends '::base.html.twig' %}

{% block inner_content -%}
    <h3>Afferenza</h3>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nome</th>
                <td>{{ entity.nome }}</td>
            </tr>
            <tr>
                <th>Descrizione</th>
                <td>{{ entity.descrizione }}</td>
            </tr>
        </tbody>
    </table>

    {# TODO add style to this form #}
    {{ form(delete_form) }}

    <a class="btn" href="{{ path('afferenza') }}">
        Back to the list
    </a>
    <a class="btn" href="{{ path('afferenza_edit', { 'id': entity.id }) }}">
        Edit
    </a>
</ul>
{% endblock %}
